# 目的別に ~/.config/zsh/*.zsh を番号順で読み込む

ZSH_CONFIG_DIR="$HOME/.config/zsh"

if [ -d "$ZSH_CONFIG_DIR" ]; then
  for f in "$ZSH_CONFIG_DIR"/[0-9][0-9]-*.zsh; do
    if [ -r "$f" ]; then
      . "$f"
    fi
  done
fi
