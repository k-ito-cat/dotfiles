# git 
abbr gs="git status"
abbr gss="git status -s"
abbr gps="git push"
abbr gpl="git pull"
abbr gd="git diff"
abbr hd="hunk diff"
abbr hdw="hunk diff --watch"
abbr hs="hunk show"

# codex
abbr codexr="codex resume"

# Codex workflow launchers
abbr gcm="codex --profile commit gcm"

abbr ww="codex --profile writing-review 'Before editing, ask only for missing details about the text type and scope, intended audience, purpose, and desired depth of editing. Wait for the reply, then use the japanese-tech-writing skill and follow its scope and workflow.'"

abbr pra="codex --profile project-readiness-audit 'Use the release-readiness-audit skill to run a full audit for this project. Inspect it read-only, follow the requested scope, and report in the skill format.'"
abbr prac="codex --profile project-readiness-audit 'Use the release-readiness-audit skill to run a change audit for this project. Inspect it read-only, follow the requested scope, and report in the skill format.'"
abbr prar="codex --profile project-readiness-audit 'Use the release-readiness-audit skill to run a release audit for this project. Inspect it read-only, follow the requested scope, and report in the skill format.'"
abbr pras="codex --profile project-readiness-audit 'Use the release-readiness-audit skill to run a security audit for this project. Inspect it read-only, follow the requested scope, and report in the skill format.'"
abbr prad="codex --profile project-readiness-audit 'Use the release-readiness-audit skill to run a docs audit for this project. Inspect it read-only, follow the requested scope, and report in the skill format.'"

# claude
abbr clauder="claude --resume"

# chezmoi
abbr czad="chezmoi add"
abbr czap="chezmoi apply"
abbr cze="chezmoi edit"
abbr czu="chezmoi update"

# terminal
abbr lg="lazygit"
abbr lsql="lazysql"
abbr ops='eval "$(op signin)"'
abbr sz="source ~/.zshrc"
abbr tb="terminal-browser"
abbr yy="yazi"

# adrs
abbr adl="adrs list -l"
abbr ads="adrs search"
abbr adh="adrs doctor"

# custom CLI
abbr gt="git today"
abbr pds="pdocs stat"

# zellij
abbr zj="zellij"
abbr zja="zellij attach -c main"
abbr zjl="zellij list-sessions"
abbr zjk="zellij kill-session"
abbr zjka="zellij kill-all-sessions"
