export OP_CONFIG_DIR="$HOME/.config/op"
export PATH="$HOME/.local/bin:$HOME/bin:$PATH"
# pdocs が読む文書リポジトリの場所。リポジトリの名前や置き場所が変わったらここだけ直す
export PDOCS_DIR="$HOME/src/github.com/k-ito-cat/project-documents"

# Ubuntuのglobal compinitはユーザー設定より先に走るため、補完初期化はzsh設定側で行う
skip_global_compinit=1
